/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      colors: {
        'redvelvet': '#ff0037',
        'wineblack': '#020000',
        'neutral-gray': '#494949',
        'discord-blue': '#5865F2',
        'light-gray': '#D9D9D9',
      },
      fontFamily: {
        sans: ['CalSans', 'system-ui', 'sans-serif'],
      },
      animation: {
        'float': 'float 6s ease-in-out infinite',
      },
      keyframes: {
        float: {
          '0%, 100%': { transform: 'translateY(0)' },
          '50%': { transform: 'translateY(-10px)' },
        }
      },
    },
  },
  plugins: [],
}